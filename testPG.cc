#include "ParticleGraphics.h"

int main() {
	ParticleGraphics pg;

	//pg.setColor("red");
//	cout << "Current color is: " << pg.getColor() << endl;
	
//	pg.drawPoint(3, 4, 80, 40 ,20);
	pg.drawLine(0, 0, 0, 11, 80, 40, 20);
//	pg.drawRectangle(20, 20, 5, 5, 80, 40 ,20);

	return 0;
}
